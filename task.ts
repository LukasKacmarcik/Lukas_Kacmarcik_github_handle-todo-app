export class Task {
  num:number;
  finished:boolean;
  name: string;

  constructor(num:number, finished:boolean, name: string) {
    this.num = num;
    this.finished = finished;
    this. name = name;
  }
}